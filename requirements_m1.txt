# M1 芯片优化的依赖安装顺序
# 首先安装基础依赖
wheel
setuptools

# PyTorch 生态系统 - M1 优化版本
torch>=2.0.0
torchvision
torchaudio

# 核心机器学习库
transformers>=4.36.0
datasets>=2.14.0
tokenizers

# 量化和 LoRA
bitsandbytes>=0.41.0  # M1 支持的版本
peft>=0.6.0

# 训练库
trl>=0.7.0
accelerate>=0.20.0

# 其他依赖
sentence-transformers>=2.2.0
numpy
pandas
scikit-learn

# 可选：如果需要 flash attention（M1 可能不支持）
# flash-attn>=2.3.0

# 其他工具
overrides>=7.4.0
gguf>=0.6.0

# 数据处理
faiss-cpu  # CPU 版本更适合 M1

# 如果需要分布式训练（M1 上可能有限制）
# deepspeed
# mpi4py
